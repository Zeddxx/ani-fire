export default function Page({
  params: { genre },
}: {
  params: { genre: string };
}) {
  return (
    <div className="wrapper-container flex h-[100svh] w-screen flex-col items-center justify-center gap-2 px-4">
      Under construction.
    </div>
  );
}
